# Launchers

Desktop launcher files live here to keep the repository root focused on agent
entrypoints and project documentation.

The launcher commands use absolute paths, so moving the `.desktop` files into
this directory does not change what they start.
