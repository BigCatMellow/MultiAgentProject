# MAP Repository Systems Gap Review

This MAP-specific review has moved to:

```text
MAP_System/artifacts/reports/MAP-repo-systems-gap-review.md
```

`Guidelines/` is reserved for universal AI collaboration protocols that apply
across projects. MAP-specific audits, reports, and state belong under
`MAP_System/`.

This compatibility pointer remains so older task records and release checklists
that cite `Guidelines/MAP_repo_systems_gap_review.md` still resolve.
