{
  "task_id": "TASK-030",
  "title": "Multi-gate handling in LangGraph runner",
  "task_type": "implementation",
  "role": "implementer",
  "status": "RELEASED",
  "dependencies": [
    "TASK-022"
  ],
  "owner": "claude",
  "description": "runner.py currently interrupts at the first pending approval gate only. Handle multiple pending gates: surface all in the interrupt payload, and resume should process each in sequence.",
  "input_paths": [],
  "output_paths": [
    "MAP_System/graph/runner.py",
    "graph/runner.py"
  ],
  "acceptance_criteria": [
    "Interrupt payload includes list of all pending gates, not just the first",
    "--approve/--reject applies to specified gate_id, then loops back if more gates remain",
    "Single-gate case unchanged (no regression)",
    "py_compile clean; runner smoke test passes"
  ]
}
