# Test Artifacts

Use this folder for focused validation notes, test evidence, and command-output
summaries that support task review.

Keep reusable validators and tests in `scripts/` and `tests/`; this folder is
for durable evidence about a task's verification, not executable test code.
